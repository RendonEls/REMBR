
<h1>this needs to work</h1>
{{#if user}}
  <section class="profile">
    <div class="bookmarks">
      <p>{{email}}</p>
      <div>
        <ul>
  {{#each user.bookmark}}
  <div class="{{_id}}">
    <a href="{{url}}">{{title}}</a>
        <li>{{text}}</li>
        {{_id}}
  </div>
  {{/each}}
  </ul>
  </div>
    </div>
  </section>
{{/if}}

<input type="submit" value="Add New" class="btn waves-effect waves-light" onclick="location.href='/user/newBookmark'">